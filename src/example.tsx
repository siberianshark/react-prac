/* import React, {createElement as e , useState} from 'react';


function App() {
  const [couut, setCount] = useState(initialState:0)
  return  e( type:'div', 
    props:{className:'container'}, 
    children:[
    e(type:'h1',props:
      {className:'font-bold', key:1}, children:`Test${count}`),
    e(type: 'button', props:
      {className:'py-2 px-4border', key:2 ,onclick:()
      => setCount(value:count+1)
    }, children:'click me'
  )
  ])
  
}

export default App;
*/